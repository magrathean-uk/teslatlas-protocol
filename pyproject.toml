[project]
name = "teslatlas-protocol-conformance"
version = "0.0.0"
description = "Local validation and conformance tooling for the Teslatlas protocol"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
  "jsonschema[format-nongpl]>=4.26,<5",
  "openapi-spec-validator>=0.7,<1",
]

[tool.uv]
package = false
